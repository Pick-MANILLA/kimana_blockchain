[profile.default]
src = "src"
out = "out"
libs = ["lib"]
test = "test"
script = "script"
solc_version = "0.8.28"
evm_version = "cancun"
optimizer = true
optimizer_runs = 10_000
# Written into every artifact so `forge inspect ... storageLayout` works off a cached build.
# Without it, a plain `forge build` produces artifacts that inspect refuses to read, and the
# CI layout check fails on a caching detail rather than on a real change. Does not affect bytecode.
extra_output = ["storageLayout"]
remappings = [
    "@openzeppelin/contracts/=lib/openzeppelin-contracts/contracts/",
    "forge-std/=lib/forge-std/src/",
]

# Fallback for a chain that has not activated Cancun, where solc's MCOPY would be an invalid opcode
# (issue #25). `make preflight` probes the target chain and says when this is needed. It produces
# different bytecode from the default profile, so build, deploy and verify all under this profile.
[profile.shanghai]
evm_version = "shanghai"

[fuzz]
runs = 1_000

[invariant]
runs = 256
depth = 50
fail_on_revert = false

[profile.ci.fuzz]
runs = 10_000

[fmt]
line_length = 120
tab_width = 4
bracket_spacing = false
int_types = "long"
quote_style = "double"
number_underscore = "thousands"

# Any EVM chain with native (6-decimal) USDC works. Testnets first, then mainnets.
[rpc_endpoints]
anvil = "http://127.0.0.1:8545"
base_sepolia = "${BASE_SEPOLIA_RPC_URL}"
arbitrum_sepolia = "${ARBITRUM_SEPOLIA_RPC_URL}"
polygon_amoy = "${POLYGON_AMOY_RPC_URL}"
sepolia = "${SEPOLIA_RPC_URL}"
base = "${BASE_RPC_URL}"
arbitrum = "${ARBITRUM_RPC_URL}"
polygon = "${POLYGON_RPC_URL}"
mainnet = "${MAINNET_RPC_URL}"

# One Etherscan (API v2) key verifies contracts on all of these chains.
[etherscan]
base_sepolia = { key = "${ETHERSCAN_API_KEY}", chain = 84532 }
arbitrum_sepolia = { key = "${ETHERSCAN_API_KEY}", chain = 421614 }
polygon_amoy = { key = "${ETHERSCAN_API_KEY}", chain = 80002 }
sepolia = { key = "${ETHERSCAN_API_KEY}", chain = 11155111 }
base = { key = "${ETHERSCAN_API_KEY}", chain = 8453 }
arbitrum = { key = "${ETHERSCAN_API_KEY}", chain = 42161 }
polygon = { key = "${ETHERSCAN_API_KEY}", chain = 137 }
mainnet = { key = "${ETHERSCAN_API_KEY}", chain = 1 }

# See more config options https://github.com/foundry-rs/foundry/blob/master/crates/config/README.md#all-options

[lint]
# `asset` mirrors the ERC-4626 naming convention; keccak256 via assembly is not worth the readability cost here.
exclude_lints = ["screaming-snake-case-immutable", "asm-keccak256"]
